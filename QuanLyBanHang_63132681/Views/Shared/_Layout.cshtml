<!DOCTYPE HTML>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="pragma" content="no-cache" />
    <meta http-equiv="cache-control" content="max-age=604800" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>Store of LÊ VĂN TIẾN - 63132681</title>

    <link href="~/Content/images/favicon.ico" rel="shortcut icon" type="image/x-icon">

    <!-- jQuery -->
    <script src="~/Content/js/jquery-2.0.0.min.js" type="text/javascript"></script>

    <!-- Bootstrap4 files-->
    <script src="~/Content/js/bootstrap.bundle.min.js" type="text/javascript"></script>
    <link href="~/Content/css/bootstrap.css" rel="stylesheet" type="text/css" />

    <!-- Font awesome 5 -->
    <link href="~/Content/fonts/fontawesome/css/all.min.css" type="text/css" rel="stylesheet">
    <link href="~/Content/fonts/fontawesome/css/all.css" rel="stylesheet" />
    <!-- custom style -->
    <link href="~/Content/css/ui.css" rel="stylesheet" type="text/css" />
    <link href="~/Content/css/responsive.css" rel="stylesheet" type="text/css" />

    <!-- custom javascript -->
    <script src="~/Content/js/script.js" type="text/javascript"></script>
    <link href="~/Content/css/ui.css" rel="stylesheet" />


    <style>
        #back-to-top {
            position: fixed;
            bottom: 20px;
            right: 20px;
            display: none;
            /* Các kiểu CSS khác cho nút, ví dụ: background, màu chữ, v.v. */
        }


        .fixed-header {
            position: fixed;
            top: 0px;
            left: 0;
            width: 100%;
            background-color: #ffd7d7;
            z-index: 1000;
            transition: top 0.3s ease;
        }

            .fixed-header.hidden {
                top: -100px;
            }

        .menu {
            box-shadow: rgba(17, 17, 26, 0.1) 0px 1px 0px, rgba(17, 17, 26, 0.1) 0px 8px 24px, rgba(17, 17, 26, 0.1) 0px 16px 48px;
        }

        .tt {
            font-size: 20px;
            text-transform: uppercase;
            color: #e8590c;
            box-shadow: rgba(240, 46, 170, 0.4) 0px 5px, rgba(240, 46, 170, 0.3) 0px 10px, rgba(240, 46, 170, 0.2) 0px 15px, rgba(240, 46, 170, 0.1) 0px 20px, rgba(240, 46, 170, 0.05) 0px 25px;
        }
    </style>
</head>
<body>

    <header class="section-header fixed-header">
        <section class="header-main border-bottom">
            <div class="container">
                <div class="row align-items-center">
                    <div class="col-xl-2 col-lg-3 col-md-12">
                        <a href="@Url.Action("Index", "Home")" class="brand-wrap">
                            @*<img class="logo" src="~/Content/images/logo.png">*@
                            <p class="font-weight-bold tt">Tiến - MobileShop</p>
                        </a> <!-- brand-wrap.// -->
                    </div>
                    <div class="col-xl-6 col-lg-5 col-md-6">
                        <form action="@Url.Action("TimKiem2", "TimKiem_63132681")" method="post" class="search-header">
                            <div class="input-group w-100">

                                <input type="text" class="form-control" placeholder="Nhập sản phẩm muốn tìm kiếm..." name="tukhoa">

                                <div class="input-group-append">
                                    <button class="btn btn-primary" type="submit">
                                        <i class="fa fa-search"></i> Search
                                    </button>
                                </div>
                            </div>
                        </form> <!-- search-wrap .end// -->
                    </div> <!-- col.// -->
                    <div class="col-xl-4 col-lg-4 col-md-6">
                        <div class="widgets-wrap float-md-right">
                            @if (Session["idUser"] != null)
                            {
                              

                                <div class="widget-header mr-3" style="text-align: center;">

                                   <a href="~/Home/Logout" class="pro">
                                      
                                       <div class="icon-area">
                                           <i class="fa fa-arrow-alt-circle-right"></i>

                                       </div>
                                       <small>Đăng xuất</small>
                                   </a>
                                    
                                </div>
                            }
                            else
                            {
                                <a href="@Url.Action("Register", "Home")" class="btn btn-secondary">Đăng Ký</a>
                                <a href="@Url.Action("Login", "Home")" class="btn btn-primary" style="margin-right: 20px">Đăng Nhập</a>
                            }
                            <div class="widget-header mr-3" style="text-align: center;">

                                <a href="@Url.Action("Details", "KhachHang_63132681", new { id = Session["idUser"]})" class="pro">
                                    <div class="icon-area">
                                        <i class="fa fa-user"></i>

                                    </div>
                                    @if (Session["idUser"] != null)
                                    {
                                        <small class="text">@Session["FullName"]</small>
                                    }
                                    else
                                    {
                                        <p> Hồ sơ</p>
                                    }

                                </a>
                            </div>


                            <div class="widget-header">
                                <a href="~/Cart_63132681/Index" class="widget-view">
                                    <div class="icon-area">
                                        <i class="fa fa-shopping-cart"></i>
                                        <span class="notify" id="CartCount">@Session["count"]</span>
                                    </div>
                                    <small class="text"> Giỏ Hàng </small>
                                </a>
                            </div>
                        </div> <!-- widgets-wrap.// -->
                    </div> <!-- col.// -->
                </div> <!-- row.// -->
            </div> <!-- container.// -->
        </section> <!-- header-main .// -->


        <nav class="navbar navbar-main navbar-expand-lg border-bottom menu">
            <div class="container">

                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#main_nav" aria-controls="main_nav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="main_nav" style="height: 60px; justify-content: center">
                    <ul class="navbar-nav">
                        <li class="nav-item ">
                            <a class="nav-link " href="@Url.Action("Index", "Home")"> @*<i class="fa fa-bars text-muted mr-2"></i>*@ Trang Chủ </a>

                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "DanhMucSP_63132681")">Sản Phẩm</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "LienHe_63132681")">Liên Hệ</a>
                        </li>

                    </ul>
     
                </div>
                <!-- collapse .//
                </div> <!-- container .// -->
        </nav>
    </header> <!-- section-header.// -->
    <div style="height: 150px;"></div>
    @RenderBody()
    <a href="#" id="back-to-top" class="btn btn-primary btn-back-to-top fa fa-chevron-up rounded-circle" style="font-size: 35px"></a>
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-lg-3 col-md-6 col-sm-6">
                    <div class="footer__about">
                        <div class="footer__logo">
                            <a href="@Url.Action("Index", "Home")">
                              
                                <img src="~/Content/images/banners/bb.jpg" style="width: 100%; height: 150px"/>
                            </a>
                        </div>
                        
                    </div>
                </div>
                <div class="col-lg-2 offset-lg-1 col-md-3 col-sm-6">
                    <div class="footer__widget">
                        <h6>Mua Sắm Thả Ga</h6>
                        <ul>
                            <li><a href="@Url.Action("Index", "DanhMucSP_63132681")">Sản Phẩm</a></li>
                            
                        </ul>
                    </div>
                </div>
                <div class="col-lg-2 col-md-3 col-sm-6">
                    <div class="footer__widget">
                        <h6>Thông tin liên lạc</h6>
                        <ul>
                            <li><a href="@Url.Action("Index", "LienHe_63132681")">Liên Hệ Chúng tôi</a></li>
                            
                        </ul>
                    </div>
                </div>
                <div class="col-lg-3 offset-lg-1 col-md-6 col-sm-6">
                    <div class="footer__widget">
                        <h6>Thư Mới</h6>
                        <div class="footer__newslatter">
                            <p>Để lại lời nhắn</p>
                            <form action="#">
                                <input type="text" placeholder="Your email">
                                <button type="submit" style="width: 50px"><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-envelope" viewBox="0 0 16 16">
                        <path d="M0 4a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2zm2-1a1 1 0 0 0-1 1v.217l7 4.2 7-4.2V4a1 1 0 0 0-1-1zm13 2.383-4.708 2.825L15 11.105zm-.034 6.876-5.64-3.471L8 9.583l-1.326-.795-5.64 3.47A1 1 0 0 0 2 13h12a1 1 0 0 0 .966-.741M1 11.105l4.708-2.897L1 5.383z" />
                            </svg></button>
                                
                            </form>
                        </div>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-lg-12 text-center">
                    <div class="footer__copyright__text">
                        <!-- Link back to Colorlib can't be removed. Template is licensed under CC BY 3.0. -->
                        <p>
                            Copyright ©
                            <script>
                                document.write(new Date().getFullYear());
                            </script> 2022
                            All rights reserved | <a href="mailto:tien.lv.63cntt@ntu.edu.vn" target="_blank">Lê Văn Tiến - 63132681</a>
                        </p>
                        <!-- Link back to Colorlib can't be removed. Template is licensed under CC BY 3.0. -->
                    </div>
                </div>
            </div>
        </div>
    </footer>
    <!-- ========================= FOOTER END // ========================= -->



</body>
</html>
<style>
    /* Định dạng thanh menu chính */
    #main_nav {
        background-color: #fff; /* Màu nền trắng */
        padding: 10px; /* Khoảng cách giữa nội dung và mép thanh menu */
    }

    /* Định dạng các mục menu */
    .navbar-nav .nav-item {
        margin-right: 10px; /* Khoảng cách giữa các mục menu */
    }

    /* Định dạng các liên kết menu */
    .navbar-nav .nav-link {
        color: #333; /* Màu chữ đen */
        font-weight: bold; /* Độ đậm của chữ */
    }

        /* Định dạng liên kết khi được hover */
        .navbar-nav .nav-link:hover {
            color: #ffcc00; /* Màu chữ khi hover */
        }

    /* Định dạng menu dropdown */
    .dropdown-menu {
        background-color: #fff; /* Màu nền trắng */
    }

    /* Định dạng mục trong menu dropdown */
    .dropdown-item {
        color: #333; /* Màu chữ đen */
    }

        /* Định dạng mục khi được hover trong menu dropdown */
        .dropdown-item:hover {
            background-color: #eee; /* Màu nền xám nhạt khi hover trong menu dropdown */
        }


    /* Phần chung cho footer */
    .footer {
        background-color: #333; /* Màu nền footer */
        color: #fff; /* Màu chữ */
        padding: 60px 0; /* Khoảng cách giữa nội dung và mép footer */
    }

    /* Phần logo và mô tả */
    .footer__about {
        margin-bottom: 30px; /* Khoảng cách giữa phần này và phần dưới */
    }

    .footer__logo img {
        max-width: 100%; /* Chỉnh kích thước logo */
    }

    /* Phần danh sách các mục */
    .footer__widget h6 {
        font-size: 18px; /* Cỡ chữ tiêu đề */
        margin-bottom: 20px; /* Khoảng cách giữa tiêu đề và danh sách */
    }

    .footer__widget ul {
        list-style: none; /* Ẩn dấu chấm của danh sách */
        padding: 0;
        margin: 0;
    }

    .footer__widget li {
        margin-bottom: 10px; /* Khoảng cách giữa các mục trong danh sách */
    }

    .footer__widget a {
        color: #fff; /* Màu chữ của các liên kết */
        text-decoration: none; /* Ẩn gạch chân của liên kết */
    }

        .footer__widget a:hover {
            text-decoration: underline; /* Hiển thị gạch chân khi hover */
        }

    /* Phần đăng ký nhận tin */
    .footer__newslatter p {
        margin-bottom: 20px; /* Khoảng cách giữa mô tả và form đăng ký */
    }

    .footer__newslatter input {
        width: 80%; /* Độ rộng của input */
        padding: 10px; /* Kích thước padding */
        margin-right: 10px; /* Khoảng cách giữa input và nút đăng ký */
    }

    .footer__newslatter button {
        background-color: #ffcc00; /* Màu nền nút */
        color: #333; /* Màu chữ nút */
        border: none; /* Loại bỏ đường viền */
        padding: 10px 20px; /* Kích thước padding của nút */
        cursor: pointer; /* Hiển thị hình con trỏ khi hover */
    }

        .footer__newslatter button:hover {
            background-color: #555; /* Màu nền nút khi hover */
        }

    /* Phần bản quyền */
    .footer__copyright__text {
        margin-top: 40px; /* Khoảng cách giữa phần trên và phần dưới */
    }

        .footer__copyright__text a {
            color: #ffcc00; /* Màu chữ của liên kết */
        }

            .footer__copyright__text a:hover {
                text-decoration: underline; /* Hiển thị gạch chân khi hover */
            }

</style>



<script>

    $(document).ready(function () {
        // Bắt sự kiện cuộn trang
        $(window).scroll(function () {
            // Kiểm tra vị trí cuộn
            if ($(this).scrollTop() > 100) {
                // Hiển thị nút "Back to Top" nếu người dùng đã cuộn xuống đủ
                $("#back-to-top").fadeIn();
            } else {
                // Ẩn nút "Back to Top" nếu người dùng cuộn lên đầu trang
                $("#back-to-top").fadeOut();
            }
        });

        // Sử dụng animate để cuộn đến đầu trang khi nút "Back to Top" được nhấn
        $("#back-to-top").click(function () {
            $("html, body").animate({ scrollTop: 0 }, 600);
            return false;
        });
    });



    $(document).ready(function () {
        var header = $(".fixed-header");
        var headerHeight = header.height();
        var isHeaderHidden = false;

        $(window).scroll(function () {
            if ($(this).scrollTop() > headerHeight) {
                if (!isHeaderHidden) {
                    header.addClass("hidden");
                    isHeaderHidden = true;
                }
            } else {
                if (isHeaderHidden) {
                    header.removeClass("hidden");
                    isHeaderHidden = false;
                }
            }
        });
    });



</script>


<script>
    $(document).ready(function () {
        $(".pro").click(function () {
            // Kiểm tra xem có đăng nhập không
            var isUserLoggedIn = @((Session["idUser"] != null).ToString().ToLower());

            if (!isUserLoggedIn) {
                // Nếu chưa đăng nhập, hiển thị thông báo và ngăn chặn chuyển trang
                alert("Bạn chưa có tài khoản. Vui lòng đăng nhập hoặc đăng ký tài khoản.");
                return false;
            }
        });
    });
</script>